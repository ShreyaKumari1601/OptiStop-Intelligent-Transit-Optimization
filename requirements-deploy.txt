# Lean dependency set for Streamlit Community Cloud deployment.
# Excludes dev/test/notebook tooling to keep build times short.
# Streamlit Cloud uses this file automatically if requirements.txt is
# replaced by it, OR point Streamlit Cloud's "requirements file" setting
# to this path explicitly. See README.md > Deployment for details.

pandas>=2.1.0
numpy>=1.26.0
scikit-learn>=1.4.0
folium>=0.15.0
matplotlib>=3.8.0
seaborn>=0.13.0
plotly>=5.18.0
streamlit>=1.32.0
streamlit-folium>=0.19.0
